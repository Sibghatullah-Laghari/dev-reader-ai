import Toolbar from "./Toolbar";

/**
 * Application header containing the main toolbar.
 */
export default function Header() {
  return (
    <header className="h-12 border-b border-zinc-800 bg-zinc-900">
      <Toolbar />
    </header>
  );
}
